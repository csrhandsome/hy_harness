# RoboTwin task references

Place reviewed successful audit JSON files and their `recipe_*.jsonl` action
traces here, grouped by task when enough validated runs exist. Runtime workers
may read matching references but must write new artifacts only to their own
episode output directory.
